# Copy to ".env" and fill in (do not commit .env).
# Spring Boot reads OS/IDE environment variables — use direnv, IDE run config,
# or "export" / setx on Windows before ./gradlew bootRun.

KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# HS256: use a long random secret (32+ bytes recommended)
JWT_SECRET=
